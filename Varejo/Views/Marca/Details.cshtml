@model Varejo.ViewModels.MarcaViewModel

@{
	ViewData["Title"] = "Detalhes da Categoria";
}

<h2>Detalhes da Marca</h2>

<div>
	<h4>@Model.NomeMarca</h4>
	<hr />
	<dl class="row">
		<dt class="col-sm-2">ID</dt>
		<dd class="col-sm-10">@Model.IdMarca</dd>

		<dt class="col-sm-2">Descrição</dt>
		<dd class="col-sm-10">@Model.NomeMarca</dd>

		<dt class="col-sm-2">Quantidade de Famílias</dt>
		<dd class="col-sm-10">@Model.QuantidadeFamilia</dd>
	</dl>
</div>

@if (Model.Familias != null && Model.Familias.Any())
{
	<h3>Famílias</h3>
	<div class="table-responsive" style="border-radius: 12px; overflow: hidden;">
		<table class="w-100">
			<thead>
				<tr>
					<th>#</th>
					<th>Descrição</th>
					<th>Status</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var familia in Model.Familias)
				{
					<tr>
						<td data-label="ID">@familia.IdFamilia</td>
						<td data-label="Família">@familia.NomeFamilia</td>
						<td data-label="Ativo">@(familia.Ativo ? "✅" : "❌")</td>
					</tr>
				}
			</tbody>
		</table>
	</div>
}
else
{
	<p>Nenhuma família cadastrada nesta marca.</p>
}

<p>
	<a class="btn btn-secondary mt-3" asp-action="Index">Voltar</a>
</p>
